"""Compatibility shim.

The project previously had a second independent Telegram bot implementation here.
The live bot is now assembled only by /main.py from /handlers/*.
This module intentionally contains no Dispatcher and no handlers.
"""
